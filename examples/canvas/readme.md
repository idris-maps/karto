# karto - canvas example

An example of usage of `karto` rendered to an HTML `<canvas>`.

[See the result](https://karto-canvas.surge.sh/)

## Try it

```
mkdir karto-canvas
cd karto-canvas
npx degit github:idris-maps/karto/examples/canvas
npm install
npm run dev
```